<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_dodgeInsetEdges="bottom">

        <TextView
            android:id="@+id/textViewFlagEmptyRecycleView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/no_contacts"
            android:textAlignment="center"
            android:textSize="30sp"
            android:textColor="#000000"
            android:layout_margin="10sp"
            android:visibility="invisible"
            app:layout_constraintBottom_toBottomOf="@+id/recycleViewContacts"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="@+id/recycleViewContacts"
            app:layout_constraintVertical_bias="0.5" />


        <!--чтобы сразу видеть наш айтем в превьюшке :
        tools:listitem="@layout/rec_view_item_contacts"-->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recycleViewContacts"
            android:layout_width="0sp"
            android:layout_height="0sp"
            android:layoutAnimation="@anim/layout_anim_fall_dawn"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHeight_percent="1"
            app:layout_constraintHorizontal_bias="0.0"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintVertical_bias="0.0"
            app:layout_constraintWidth_percent="1"

            tools:listitem="@layout/rec_view_item_contacts"/>


    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>